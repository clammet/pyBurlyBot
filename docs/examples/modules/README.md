# Example modules

These files demonstrate module APIs and are intentionally outside
`pyburlybot_modules`, so they are not discoverable as production plugins.
Copy an example into that package before enabling it.
