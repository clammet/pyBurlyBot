# Special-purpose modules

These modules are retained as reference implementations for narrow deployments.
They are intentionally outside `pyburlybot_modules`; review and copy one into the
plugin package only when its network-specific assumptions are appropriate.
